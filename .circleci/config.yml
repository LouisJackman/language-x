version: 2

jobs:
  build:
    docker:
      - image: circleci/rust

    steps:
      - checkout
      - restore_cache:
          key: project-cache
      - run:
          name: Verify and Build on Stable
          command: make
      - run:
          name: Verify and Build on Beta
          command: make RUST_VERSION=beta
      - run:
          name: Upload Coverage
          command: ./scripts/codecov.sh
      - save_cache:
          key: project-cache
          paths:
            - "~/.cargo"
            - "~/.rustup"
            - "./target"

